'use strict';
const React = require('react');
const { render, Box, Text } = require('ink');

const Cli = () => {
  return (
    <Box>
      <Text>LIMP BIZKIT</Text>
    </Box>
  );
};

render(<Cli />);
